#include<stdio.h>

#include<limits.h>

int main()

{
	printf("The smallest integer value that can be stored is %d\n", INT_MIN);
	
	printf("The largest value that can be stored is %d\n", INT_MAX);
	
	return 0;
	
}