#include<stdio.h>
int main()
{
	int length, width, depth, volume;
	length= 25;
	width= 10;
	depth= 6;
	volume= (length*width*depth);
	printf("The volume of swimming pool is %d cubic meter", volume);
	return 0;
}